# TorShield — Python dependencies
# Install with:  pip install -r requirements.txt

customtkinter>=5.2.0
stem>=1.8.1
requests>=2.31.0
PySocks>=1.7.1        # SOCKS5 support for requests
fake-useragent>=1.4.0 # Optional — User-Agent randomisation
Pillow>=10.0.0        # Logo image display in the header
