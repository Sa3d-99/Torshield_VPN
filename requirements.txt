# TorShield — Python dependencies (pinned for reproducible installs)
# Install with:  pip install -r requirements.txt
#
# Versions are pinned with a compatible-release range so the SAME working
# environment is reproduced on every machine (your friend's laptop included),
# while still allowing security patch releases.

# ── GUI (PyQt-Fluent-Widgets edition, v2.2) ──────────────────────────────────
PyQt5>=5.15.2,<6.0             # Qt bindings the Fluent UI is built on
PyQt-Fluent-Widgets>=1.5.0     # Fluent Design widgets (soft shadows, theming)
# NOTE: on Linux this also needs the system package python3-pyqt5.qtx11extras
#       (installed automatically by install.sh).

stem>=1.8.2,<2.0               # Tor control-port library (circuits, NEWNYM)
requests>=2.31.0,<3.0          # Connection test over the Tor SOCKS proxy
PySocks>=1.7.1,<2.0            # SOCKS5 support for requests (socks5h://)
fake-useragent>=1.5.1,<2.0     # Optional — User-Agent randomisation
Pillow>=10.2.0,<12.0           # Logo image display in the header

# ── Windows only (ignored on Linux via the platform marker) ──────────────────
pydivert>=2.1.0; sys_platform == "win32"   # WinDivert bindings for all-TCP routing
